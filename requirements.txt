# FastAPI backend runtime
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Database - versions resolved by langgraph-checkpoint-mongodb requirements
motor>=3.7,<4
pymongo>=4.12,<4.18

# Gemini LLM integration + LangGraph agent
# Let pip resolve the compatible set across the whole ecosystem
langchain-google-genai
langchain-openai
langchain-core
langgraph
langgraph-checkpoint-mongodb==0.2.0

# Long-term memory
mem0ai

# Small/fast summarizer (OpenAI-compatible endpoints: GROQ / NVIDIA)
# Used to compress mem0 data, tool payloads, and sliding-window history
# before they enter the main LLM context window.
openai>=1.0.0

# Forecasting model runtime
catboost==1.2.8

# Vector Database (Pinecone)
pinecone-client
langchain-pinecone

# Web search / scraping (Tavily & Firecrawl)
tavily-python
firecrawl-py

# Observability
prometheus-client==0.19.0
structlog==24.1.0
